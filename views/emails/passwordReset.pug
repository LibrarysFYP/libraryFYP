extends baseEmail

block content
    // CONTENT
    p(style="color:black") Hi #{firstName},
    p(style="color:black") Forgot your password? Submit this verification code.
    span(style="background-color: green; color:white; padding:10px; font-size:18px; border-radius:5px") #{resetcode}